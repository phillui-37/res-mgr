#!/usr/bin/env bash
# bin/console — open an interactive Ruby console inside the dev container
set -euo pipefail

command -v podman &>/dev/null || { echo "ERROR: podman not found." >&2; exit 1; }

exec podman compose run --rm dev gel exec irb -r ./config.ru
