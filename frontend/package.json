{
  "name": "res-mgr-frontend-root",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm --filter @res-mgr/web dev",
    "build:web": "pnpm --filter @res-mgr/web build",
    "build:electron": "pnpm build:web && pnpm --filter @res-mgr/electron build",
    "build": "pnpm build:web && pnpm build:electron",
    "preview": "pnpm --filter @res-mgr/web preview",
    "test": "pnpm --filter @res-mgr/web test"
  }
}
